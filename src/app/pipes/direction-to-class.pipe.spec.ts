import { DirectionToClassPipe } from './direction-to-class.pipe';

describe('DirectionToClassPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectionToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
